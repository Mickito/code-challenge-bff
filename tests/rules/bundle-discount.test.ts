import { BundleDiscount } from "../../src/rules";

describe("BundlePricingRule", () => {
  const THRESHOLD = 3;
  const QUANTITY = 2;
  const bundleDiscount = new BundleDiscount("ipd", THRESHOLD, QUANTITY);

  test("should apply ");
});
